# spinlock-fun

(I've only tested this on MacOS, and definitely won't work on Windows)

## Requirements

- CMake
- C++ compiler

## Build & run

```
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .
./deadly_spin
```
